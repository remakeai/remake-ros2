# Additional dependencies for Remake AI Appstore integration
# Install these to enable robot-to-appstore connectivity

# Socket.IO client for real-time communication with appstore backend
python-socketio[asyncio_client]>=5.0.0

# Note: The base remakeai requirements.txt should already include:
# - websockets>=12.0
# - Pillow>=10.1.0
# - ROS2 dependencies (installed via rosdep)
